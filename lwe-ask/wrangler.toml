name = "lwe-ask"
main = "src/index.js"
compatibility_date = "2026-08-07"

# Temporary migration adapter. Delete this Worker and its routes after production
# logs show the retired /lwe/ask API is no longer called.
routes = [
  { pattern = "aadhar.sh/lwe/ask", zone_name = "aadhar.sh" },
  { pattern = "aadhar.sh/lwe/ask/*", zone_name = "aadhar.sh" }
]

[observability]
enabled = true
