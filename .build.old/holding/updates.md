---
title: "Windows Update · aadhar.sh"
description: "What has shipped to this site lately, in a Windows Update reskin. Read-only."
path: "/updates"
section: "status"
kind: "page"
source: "https://aadhar.sh/updates"
---

> Site index: https://aadhar.sh/llms.txt
> This is the Markdown twin of a page on aadhar.sh. The HTML at the source
> URL below is the original, and is hand-written and unminified on purpose.

# Windows Update

**aadhar.sh is up to date.**

running build aadhar-v162-hit-route. updates install themselves at deploy time, so you never click "restart now."

## Recently installed

- hit-routecounter tick endpoint renamed /hit.svg to /hit
- desktop-iconsDesktop icons: fix the collision-repair stampede that stacked every profile shortcut
- understanding-checksUnderstanding checks: quizzes on every garage and LWE page, lens pedagogy pass
- audit-followupsAudit followups: /around obeys robots.txt, RRULE expansion in coffee, slashless URLs, signed Spotify scrape, Delta + readiness fixes
- lens-economicsLens: context economics readout + agent-door probes
- perf-passPerf pass: repaired luna.css + unbypassable minified build, responsive photo tiers, edge-cached /around,/updates,/restore, coffee SWR with fail-closed booking
- agentic-termsLens Terms tab + x402 paywall on llms-full.txt + the crawl ledger
- resume-machine-readableMachine-readable resume mirrors (text + JSON Resume) for agents; refreshed the PDF
- dac-audio-edge-cacheLive audio on every DAC demo (real-time knobs), plus a version-keyed edge cache
- dac-live-audioDAC LWE: live audio on every demo + real-time tube
- tube-distortionDAC LWE: tube distortion demo (even vs odd harmonics)
- deploy-tripwiresDeploy-time invariant tripwires: allowlist mirror, CSP style-src, and the VT blink-fix now block a bad deploy
- finish-hash-migrationFinish the content-hash migration: THUMB\_VERSION and the legacy ?v= fallback retire
- discovery-surfacesA static document joins the sitemap + llms.txt discovery surfaces
- static-docA mutable static document, served with a replace-in-place cache rule
- back-to-parentA blank Back button now walks up to the page parent; real history still rides bfcache first
- no-white-blinkThe window morph stops blinking white: its snapshot pair leaves plus-lighter for normal alpha compositing
- taskbar-truthThe active app taskbar button sits depressed; mobile taskbar unsquished; icon drag goes compositor-only for 120Hz

No reboot, no nagging. Each item is a real deploy from the site's checkpoint log; see how delivery works in [Security Center](https://aadhar.sh/security), or roll the whole system back through every past build in [System Restore](https://aadhar.sh/restore).

Source: https://aadhar.sh/updates
