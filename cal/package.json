{
  "name": "cal-aadhar-sh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.toml",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy -c wrangler.toml",
    "tail": "wrangler tail -c wrangler.toml",
    "secret:set": "wrangler secret put -c wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.15",
    "vitest": "^4.1.9",
    "wrangler": "^4.102.0"
  }
}
