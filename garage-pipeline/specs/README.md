# Garage specs

Put one `<id>.json` file here for each entry in `../pages.json`. The generator
validates the shared editorial and understanding contract before it writes a
page. See [`../README.md`](../README.md) for the spec shape and commands.
