/*! minified at deploy - readable source: /lens-webmcp.src.js */
const VIEWS=Object.freeze([`both`,`human`,`machine`,`browser`,`delta`]),OPT_IN=Object.freeze([`reader`,`wire`,`tools`,`nlweb`,`markdown`]),SWITCHES=Object.freeze([`markdown`,`semantic`,`contract`,`authority`,`receipt`,`dictionary`,`ech`]);let loadClient=null,handlers=null;export function installHandlers(e){handlers=e}window.LensWebMcp=Object.freeze({installHandlers,views:VIEWS,optIn:OPT_IN,switches:SWITCHES});async function execute(e,t){if(!loadClient)throw Error(`The Lens client loader is unavailable.`);await loadClient();let n=handlers&&handlers[e];if(!n)throw Error(`The Lens client did not publish the `+e+` handler.`);return n(t)}const proxy=e=>t=>execute(e,t);export async function boot(e){loadClient=e;let t=await import(`/webmcp.js`);if(!t.available())return;let n=[...document.querySelectorAll(`.lx-tab`)].map(e=>e.getAttribute(`data-lens`)).filter(e=>e),r=[{name:`lens_scan`,description:`Scan a URL in the Lens window on this page, the one the person is watching. The panes repaint as it runs and this returns once the scan has settled. Prefer lens_inspect when you only want the data: that one answers you privately and leaves the screen alone. Use this one when the person should SEE what you found.`,inputSchema:{type:`object`,properties:{url:{type:`string`,description:`an absolute http(s) URL`}},required:[`url`]},annotations:{readOnlyHint:!1,idempotentHint:!1,openWorldHint:!0},writes:!1,execute:proxy(`lens_scan`)},{name:`lens_compare_onscreen`,description:`Put two URLs through the same rubric side by side, in the Lens window on this page. Returns the comparison and leaves it on screen. The head-to-head costs a tighter per-visitor budget than a single scan, so do not loop it.`,inputSchema:{type:`object`,properties:{left:{type:`string`},right:{type:`string`}},required:[`left`,`right`]},annotations:{readOnlyHint:!1,idempotentHint:!1,openWorldHint:!0},writes:!1,execute:proxy(`lens_compare_onscreen`)},{name:`lens_show`,description:"Change what the Lens window is displaying. `pane` picks the split ("+VIEWS.join(`, `)+") and `tab` picks which machine lens is open ("+n.join(`, `)+`). This only moves the view. Five tabs hold nothing until somebody pays for the fetch, so follow it with lens_run_tab when the pane comes back empty.`,inputSchema:{type:`object`,properties:{pane:{type:`string`,enum:VIEWS},tab:{type:`string`,enum:n}}},annotations:{readOnlyHint:!1,idempotentHint:!0,openWorldHint:!1},writes:!1,execute:proxy(`lens_show`)},{name:`lens_run_tab`,description:`Pay for one of the opt-in machine tabs and wait for it: reader (a third-party extractor's guess at the article), wire (every request the page makes, through a real browser), tools (the MCP catalog the origin advertises), nlweb (ask the origin's own /ask endpoint a question), markdown (replay the Accept header each named agent client sends, and report which representation each one gets). Each one is a fresh fetch of somebody else's site from this visitor's budget, which is why none of them run on their own. Needs a scan on screen first.`,inputSchema:{type:`object`,properties:{tab:{type:`string`,enum:OPT_IN},query:{type:`string`,description:`nlweb only: the question to ask the origin`}},required:[`tab`]},annotations:{readOnlyHint:!1,idempotentHint:!1,openWorldHint:!0},writes:!1,execute:proxy(`lens_run_tab`)},{name:`lens_read_screen`,description:`Read what the Lens window is showing right now: the URL that was scanned, its status, which pane and machine tab are open, the agent-readiness score, which Delta switches are flipped, and which opt-in tabs already hold data. The one tool here that changes nothing. Call it to find out what the person is looking at before you touch anything.`,inputSchema:{type:`object`,properties:{}},annotations:{readOnlyHint:!0,idempotentHint:!0,openWorldHint:!1},writes:!1,execute:proxy(`lens_read_screen`)},{name:`lens_delta`,description:`Flip one of the Delta counterfactual switches and repaint. Each switch simulates a fix the scanned site has not made (`+SWITCHES.join(`, `)+`) and shows what it would have bought. Nothing is sent anywhere: the whole simulation is arithmetic on the scan already on screen.`,inputSchema:{type:`object`,properties:{switch:{type:`string`,enum:SWITCHES},on:{type:`boolean`,description:`omit to toggle`}},required:[`switch`]},annotations:{readOnlyHint:!1,idempotentHint:!0,openWorldHint:!1},writes:!1,execute:proxy(`lens_delta`)}];for(let e of r)await t.registerTool(e)}