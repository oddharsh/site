# Pillow is for gen-pixel-peeper.py, which composites the /pixel-peeper
# comparison frames (Image, ImageChops, ImageDraw, ImageFilter, ImageOps,
# ImageStat). It is a one-off page generator, NOT part of add-photos.sh.
#
# The histogram bake used to live here too and moved into zenc on 2026-08-14
# (`zenc histogram`), and the captioner moved to gen-alt-text.ts on 2026-09-15,
# so the CORE photo pipeline runs no Python at all: this file is for the one
# generator that still does. build-recipes.py was the third consumer and was
# deleted on 2026-08-14 when exif-sooc took over the recipe card, which
# config/retired.json records under `exiftool`.
Pillow==12.3.0
